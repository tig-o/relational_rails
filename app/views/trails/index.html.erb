<h1>Trails</h1>

<% @trails.each do |trail| %>
    <h3><p>Trail Name: <%= trail.name %></p></h3>
    <p>Difficulty: <%= trail.difficulty %></p>
    <p>Length: <%= trail.length %></p>
    <p>Elevation Gain: <%= trail.elevation_gain %> feet</p>
    <p>Dogs Allowed? <%= trail.dogs_allowed %> miles</p>
<% end %>
